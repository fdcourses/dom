'use strict';

/**
 * Функция сложения чисел
 * @param {number} a первое число
 * @param {number} b второе число
 * @returns {number | Array} сумма чисел
 */
function sum(a,b) {
  return a+b;
}
sum(a,b);
